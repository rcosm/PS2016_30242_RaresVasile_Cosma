@model IEnumerable<PS02.Models.show>

@{
    ViewBag.Title = "Shows Index";
}

<h2>Shows Index</h2>

<p>
    @Html.ActionLink("Create New Show", "Create")
</p>
<p>
    @Html.ActionLink("Users Index", "Index", "Users")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.administration)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.distribution)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.premiere)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nrOfTickets)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.administration)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.distribution)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.premiere)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nrOfTickets)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
